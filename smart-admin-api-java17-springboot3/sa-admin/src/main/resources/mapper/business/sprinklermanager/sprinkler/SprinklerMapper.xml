<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.lab1024.sa.admin.module.business.sprinklermanager.sprinkler.dao.SprinklerDao">
    <select id="queryPage"
            resultType="net.lab1024.sa.admin.module.business.sprinklermanager.sprinkler.domain.vo.SprinklerVO">
        SELECT t_sprinkler.*
        FROM t_sprinkler
        <where>
            deleted_flag = #{queryForm.deletedFlag}
            <if test="queryForm.purchaseDateContractNumber != null and queryForm.purchaseDateContractNumber != ''">
                AND INSTR(purchase_date_contract_number,#{queryForm.purchaseDateContractNumber})
            </if>
            <if test="queryForm.sprinklerModel != null and queryForm.sprinklerModel != ''">
                AND INSTR(sprinkler_model,#{queryForm.sprinklerModel})
            </if>
            <if test="queryForm.sprinklerSerial != null and queryForm.sprinklerSerial != ''">
                AND INSTR(sprinkler_serial,#{queryForm.sprinklerSerial})
            </if>
            <if test="queryForm.shippingDateStartTime != null">
                AND DATE_FORMAT(shipping_date, '%Y-%m-%d') &gt;= #{queryForm.shippingDateStartTime}
            </if>
            <if test="queryForm.shippingDateEndTime != null">
                AND DATE_FORMAT(shipping_date, '%Y-%m-%d') &lt;= #{queryForm.shippingDateEndTime}
            </if>
            <if test="queryForm.warehouseDateStartTime != null">
                AND DATE_FORMAT(warehouse_date, '%Y-%m-%d') &gt;= #{queryForm.warehouseDateStartTime}
            </if>
            <if test="queryForm.warehouseDateEndTime != null">
                AND DATE_FORMAT(warehouse_date, '%Y-%m-%d') &lt;= #{queryForm.warehouseDateEndTime}
            </if>
            <if test="queryForm.allocateDateStartTime != null">
                AND DATE_FORMAT(allocate_date, '%Y-%m-%d') &gt;= #{queryForm.allocateDateStartTime}
            </if>
            <if test="queryForm.warehouseDateEndTime != null">
                AND DATE_FORMAT(allocate_date, '%Y-%m-%d') &lt;= #{queryForm.allocateDateStartTime}
            </if>
            <if test="queryForm.history != null and queryForm.history != ''">
                AND INSTR(history,#{queryForm.history})
            </if>
            <if test="queryForm.disabledFlag != null">
                AND disabled_flag = #{queryForm.disabledFlag}
            </if>
        </where>
        <if test="queryForm.sortItemList == null or queryForm.sortItemList.size == 0">
            ORDER BY create_time DESC
        </if>
    </select>
    <select id="queryBySprinklerSerial"
            resultType="net.lab1024.sa.admin.module.business.sprinklermanager.sprinkler.domain.entity.SprinklerEntity">
            SELECT *
            FROM t_sprinkler
            WHERE sprinkler_serial = #{sprinklerSerial}
            AND deleted_flag = #{deletedFlag}
            <if test="excludeSprinklerId != null">
                AND sprinkler_id != #{excludeSprinklerId}
            </if>
    </select>
    <select id="getDetail"
            resultType="net.lab1024.sa.admin.module.business.sprinklermanager.sprinkler.domain.vo.SprinklerVO">
        SELECT s.*
        FROM t_sprinkler s
        WHERE s.sprinkler_id = #{sprinklerId} AND
              s.deleted_flag = #{deletedFlag}
    </select>

    <update id="deleteSprinkler">
        UPDATE t_sprinkler
        SET deleted_flag = #{deletedFlag}
        WHERE sprinkler_id = #{sprinklerId}
    </update>

</mapper>